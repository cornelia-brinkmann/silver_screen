models:
  - name: int_location
    description: merge of all three locations
    columns:
    - name: movie_id
      description: primary key
      tests: # tests that would be run on the column
        - unique
        - not_null
  
  - name: int_rental_cost
    description: merge of movvies with invoices
    columns:
    - name: movie_id
      description: primary key
      tests: # tests that would be run on the column
        - unique
        - not_null

  
  

  
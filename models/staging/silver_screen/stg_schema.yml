version: 2

models:
  - name: stg_silver_screen__invoices
    description: description
    columns:
    - name: movie_id
      description: primary key
      tests: # tests that would be run on the column
        - not_null

    - name: invoice_id
      description: description
      tests: # tests that would be run on the column
        - unique
        - not_null
  
  - name: stg_silver_screen__invoices_dedup
    columns:
      - name: invoice_id
        tests:
          - not_null
          - unique
  
  - name: stg_silver_screen__movies
    description: description
    columns:
    - name: movie_id
      description: primary key
      tests: # tests that would be run on the column
        - unique
        - not_null

  - name: stg_silver_screen__nj_001
    description: description
    columns:
      - name: movie_id
        description: primary key
        tests: # tests that would be run on the column
          - not_null

  - name: stg_silver_screen__nj_002
    description: description
    columns:
    - name: movie_id
      description: primary key
      tests: # tests that would be run on the column
        - not_null

  - name: stg_silver_screen__nj_003
    description: description
    columns:
    - name: movie_id
      description: primary key
      tests: # tests that would be run on the column
        - not_null
